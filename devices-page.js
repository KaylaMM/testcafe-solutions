import { Selector, t } from "testcafe";

const name = Selector(".device-name");
const type = Selector(".device-type");
const capacity = Selector(".device-capacity");
const editButton = Selector(".device-edit");
const removeButton = Selector(".device-remove");

class Devices {
  constructor(text) {}
}
